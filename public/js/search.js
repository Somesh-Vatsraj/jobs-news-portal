// Search enhancements — debounced suggest endpoints are optional.
// Currently the search is a plain server-rendered form (progressive).
(function () {
  'use strict';
  // Kept minimal to preserve performance. Any progressive enhancement
  // for search autocomplete can be added here later using /api/jobs?q=...
})();
