{
  "name": "jobs-news-portal",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 migrations apply jobs-news-db --local",
    "migrate:remote": "wrangler d1 migrations apply jobs-news-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
